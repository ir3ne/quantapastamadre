import React from 'react';
import classNames from './Separator.module.scss';

const Separator = () => {
  return (
    <>
      <hr className={classNames.separator} />
    </>
  );
}

export default Separator;